name: advannet_client
description: Dart client library for the Keonn AdvanNet REST API (AdvanReader, AdvanSafe, AdvanPay, AdvanFlow devices).
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.2.0
  xml: ^6.5.0
  meta: ^1.15.0
  crypto: ^3.0.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
